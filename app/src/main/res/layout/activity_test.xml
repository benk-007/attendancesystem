<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp"
    android:background="@color/background_color">

    <!-- Boutons de test -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:layout_marginBottom="16dp">

        <Button
            android:id="@+id/btn_test_firebase"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="Test Firebase"
            android:layout_marginEnd="4dp"
            android:textSize="12sp" />

        <Button
            android:id="@+id/btn_test_google_drive"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="Test G-Drive"
            android:layout_marginStart="4dp"
            android:layout_marginEnd="4dp"
            android:textSize="12sp" />

    </LinearLayout>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:layout_marginBottom="16dp">

        <Button
            android:id="@+id/btn_test_models"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="Test Modèles"
            android:layout_marginEnd="4dp"
            android:textSize="12sp" />

        <Button
            android:id="@+id/btn_clear_tests"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="Effacer"
            android:layout_marginStart="4dp"
            android:textSize="12sp"
            android:backgroundTint="@color/warning_color" />

    </LinearLayout>

    <!-- Zone de résultats -->
    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Résultats des Tests"
        android:textStyle="bold"
        android:textSize="16sp"
        android:textColor="@color/text_primary"
        android:layout_marginBottom="8dp" />

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:background="@color/surface_color"
        android:padding="12dp">

        <TextView
            android:id="@+id/tv_test_results"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Appuyez sur un bouton pour lancer les tests..."
            android:textSize="12sp"
            android:fontFamily="monospace"
            android:textColor="@color/text_primary"
            android:lineSpacingExtra="2dp" />

    </ScrollView>

    <!-- Instructions -->
    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="💡 Conseils:\n• Testez Firebase en premier\n• Connectez-vous à Google Drive si nécessaire\n• Les modèles testent la structure des données"
        android:textSize="12sp"
        android:textColor="@color/text_secondary"
        android:layout_marginTop="8dp"
        android:background="@color/primary_color_light"
        android:padding="8dp" />

</LinearLayout>